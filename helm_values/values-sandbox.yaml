## yarilo sandbox — mail-sb.seconddns.com
##
## Pre-reqs:
##   1. cert-manager + ClusterIssuer "letsencrypt-prod" in the cluster.
##   2. DNS A/AAAA for mail-sb.seconddns.com pointing at the LB IP.
##   3. MySQL in yarilo-sb namespace (apply mysql-sandbox.yaml first).
##
## Install:
##   helm upgrade --install yarilo ./helm \
##     -f helm_values/values-sandbox.yaml \
##     -n yarilo-sb --create-namespace

hostname: mail-sb.seconddns.com

storage:
  volatileDir: "/tmp/yarilo-volatile/%d/%n"
  persistence:
    enabled: true
    accessMode: ReadWriteOnce
    size: 10Gi

## Enable XCLIENT so session pods accept real-client-IP forwarding from login pods.
general:
  limits:
    maxUserIPConnections: 50
  xclient:
    enabled: true
    trustedNets:
      - "10.0.0.0/8"
  limits:
    maxUserIPConnections: 0

image:
  repository: ghcr.io/0kaba0hub/yarilo
  tag: latest
  pullPolicy: Always

logLevel: debug

service:
  type: LoadBalancer

protocol:
  imap:
    idleNotifyInterval: 120
    maxLineLength: 65536
    acl:
      enabled: true
  lmtp:
    addReceivedHeader: true
    hdrDeliveryAddress: final
  managesieve:
    maxScriptSize: 65536

sieve:
  enabled: true
  defaultName: "yarilo"
  maxScriptSize: 65536
  maxRedirects: 32
  vacationEnabled: true
  submission:
    host: "relay-fwd.yarilo-relay.svc.cluster.local:25"
    ssl: "no"
    authSecret: "sieve-smtp-auth"
  globalBefore: []
  globalAfter: []
  sieveExtensions:
    - fileinto
    - reject
    - ereject
    - envelope
    - encoded-character
    - variables
    - relational
    - subaddress
    - body
    - date
    - index
    - mailbox
    - duplicate
    - imap4flags
    - redirect
    - copy
    - vacation
    - enotify
    - vnd.yarilo.debug

dicts:
  metadata:
    driver: redis
    settings:
      addr: yarilo-redis.yarilo-sb.svc.cluster.local:6379
      db: 0
      prefix: "yarilo:metadata:"
      path: null
  quota:
    driver: redis
    settings:
      addr: yarilo-redis.yarilo-sb.svc.cluster.local:6379
      db: 0
      prefix: "yarilo:quota:"
    expire_secs: 86400
  sieve_vacation:
    driver: redis
    settings:
      addr: yarilo-redis.yarilo-sb.svc.cluster.local:6379
      db: 0
      prefix: "yarilo:sieve:vacation:%u:"

## External MySQL — managed by mysql-sandbox.yaml; bundled StatefulSet disabled.
mysql:
  bundled: false

## DSN injected from the yarilo-mysql Secret created by mysql-sandbox.yaml.
extraEnv:
  - name: YARILO_DB_DSN
    valueFrom:
      secretKeyRef:
        name: yarilo-mysql
        key: dsn

components:
  ## IMAP login — TLS terminator on :993 (IMAPS) / :143 (STARTTLS).
  ## Creates yarilo-tls Secret via cert-manager.
  imapLogin:
    enabled: true
    tls:
      secretName: yarilo-tls
      certificate:
        enabled: true
        issuerRef:
          kind: ClusterIssuer
          name: letsencrypt-prod
        dnsNames:
          - mail-sb.seconddns.com
    listeners:
      imaps:
        enabled: true
      imap:
        enabled: true
    backendAddr: "yarilo-imap:10143"
    service:
      type: LoadBalancer
      annotations:
        metallb.universe.tf/loadBalancerIPs: "10.50.80.29"
        metallb.universe.tf/allow-shared-ip: "yarilo-mail"

  ## POP3 login — TLS terminator on :995 (POP3S) / :110 (STLS).
  ## Reuses yarilo-tls Secret from imapLogin.
  pop3Login:
    enabled: true
    tls:
      secretName: yarilo-tls
    listeners:
      pop3s:
        enabled: true
      pop3:
        enabled: true
    backendAddr: "yarilo-pop3:10110"
    service:
      type: LoadBalancer
      annotations:
        metallb.universe.tf/loadBalancerIPs: "10.50.80.29"
        metallb.universe.tf/allow-shared-ip: "yarilo-mail"

  auth:
    passdb:
      - driver: mysql
        dsn: "${YARILO_DB_DSN}"
        defaultPassScheme: BCRYPT
        ## PostfixAdmin mailbox table — skipSchema prevents yarilo-auth from
        ## creating its own yarilo_users table.
        ## Columns matched by name — only "password" is required.
        ## "enabled" maps to PostfixAdmin's "active"; absent columns default active.
        skipSchema: true
        passwordQuery: |
          SELECT password, active AS enabled, allow_nets
          FROM mailbox
          WHERE username = %u
        userQuery: |
          SELECT CONCAT(home, mpath) AS home,
                 CONCAT(mbtype, ':', home, mpath) AS mail
          FROM mailbox
          WHERE username = %u
        iterateQuery: |
          SELECT username
          FROM mailbox
          WHERE active = 1
          ORDER BY username

    token:
      backend: redis
      redisAddr: "redis://yarilo-redis.yarilo-sb.svc.cluster.local:6379/0"

  ## IMAP session pod.
  imap:
    listeners:
      imap:
        enabled: true

  ## POP3 session pod.
  pop3:
    enabled: true
    listeners:
      pop3:
        enabled: true

  ## LMTP backend — receives preamble-wrapped connections from lmtp-login.
  lmtp:
    enabled: true

  ## LMTP login proxy — MTA-facing, plain LMTP, no preamble required.
  lmtpLogin:
    enabled: true
    backendAddr: "yarilo-lmtp:10024"

  saslLogin:
    enabled: true
    service:
      type: LoadBalancer
      annotations:
        metallb.universe.tf/loadBalancerIPs: "10.50.80.29"
        metallb.universe.tf/allow-shared-ip: "yarilo-mail"

  quotaStatus:
    enabled: true
    authMasterAddr: "yarilo-auth:9102"
    service:
      type: LoadBalancer
      annotations:
        metallb.universe.tf/loadBalancerIPs: "10.50.80.29"
        metallb.universe.tf/allow-shared-ip: "yarilo-mail"

  ## ManageSieve backend — internal, not exposed outside cluster.
  manageSieve:
    enabled: true

  ## ManageSieve login pod — client-facing on :4190 STARTTLS.
  manageSieveLogin:
    enabled: true
    backendAddr: "yarilo-managesieve:14190"
    tls:
      secretName: yarilo-tls
    listeners:
      managesieve:
        enabled: true
    service:
      type: LoadBalancer
      annotations:
        metallb.universe.tf/loadBalancerIPs: "10.50.80.29"
        metallb.universe.tf/allow-shared-ip: "yarilo-mail"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 1000m
    memory: 512Mi
